import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryDark = Color(0xFF1A3A52);
  static const Color primaryBlue = Color(0xFF2C5F7F);

  // Background colors
  static const Color backgroundLight = Color(0xFFF5F5F5);
  static const Color white = Color(0xFFFFFFFF);

  // Text colors
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textGray = Color(0xFF757575);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Accent colors
  static const Color accent = Color(0xFFFFD700);
  static const Color heartRed = Color(0xFFFF3B30);

  // Button colors
  static const Color buttonPrimary = Color(0xFF1A3A52);
  static const Color buttonOutline = Color(0xFFE0E0E0);
}